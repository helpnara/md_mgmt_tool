# 사내 AX 성과공유회 포스터

세로형 **A1(594 × 841mm)** 포스터. 파워포인트와 PDF 두 벌을 함께 둔다.

| 파일 | 용도 |
|---|---|
| `AX성과공유회_포스터_개발을기다리지않았다.pptx` | 제출·수정용. 회사 PC의 파워포인트에서 그대로 열린다 |
| `AX성과공유회_포스터_개발을기다리지않았다.pdf` | 인쇄·배포용 (A1 세로, 1쪽) |
| `make_poster.py` | 포스터를 만드는 스크립트. 수치를 고칠 일이 생기면 이 파일의 `DATA` 만 고친다 |
| `screen-*.png` | 포스터에 들어간 실제 화면 갈무리 (데모 데이터) |

## 글꼴

파워포인트 쪽은 **맑은 고딕**으로 지정했다. 회사 윈도우 PC에는 기본으로 있으므로
따로 설치할 것이 없다. PDF 는 리눅스에서 변환하느라 나눔고딕으로 렌더링되어
글자 모양이 아주 조금 다르지만, 자리와 크기는 같다.
**파워포인트에서 직접 [다른 이름으로 저장 → PDF]** 하면 맑은 고딕 그대로 나온다.

## 다시 만들기

```bash
python make_poster.py                       # pptx 생성 (python-pptx 필요)
soffice --headless --convert-to pdf --outdir . <파일>.pptx   # PDF 변환
```

## 숫자의 출처

포스터의 규모·기간·시험 건수는 모두 실측치이고, 외주 비용은 추정치다.
산출 과정은 [`../외주개발-비용산정.md`](../외주개발-비용산정.md) 에 있다.
**단가는 가정값**이므로 발표에 인용하기 전에 최신 공표치로 바꿔야 한다.
