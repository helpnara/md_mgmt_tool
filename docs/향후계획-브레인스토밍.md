# 향후 계획 브레인스토밍

> 2026-09-02 작성 · 로드맵(R1~R3)을 **실행 단위로 쪼개고**, 로드맵에 없던 것까지 넓게 펼쳐 본 목록.
> 이 문서는 **고르기 위한 목록**이다. 채택한 것만 [TODO.md](TODO.md) 로 옮겨 구현한다.
>
> **규모** S = 반나절 이내 · M = 1~3일 · L = 1주 이상
> **효과** ★★★ 매일 체감 · ★★ 자주 체감 · ★ 가끔이지만 없으면 곤란

---

## 0. 한눈에 — 추천 3단계

성과공유회 발표에 그대로 쓸 수 있는 묶음이다. 각 단계의 근거는 아래 항목에 있다.

| 단계 | 시점 | 무엇을 | 왜 지금인가 |
|---|---|---|---|
| **1단계 — 쓰면서 바로 아쉬운 것** | 2주 | **T8 과제 효과 항목** · T5 보고 시점 마커 · T1 엑셀 표 붙여넣기 · T3 보고 양식 설정 · T6 삭제 복구 · T2 진행일지 템플릿 | 혼자 쓰는 지금 매일 부딪히는 것들. T8 은 스키마가 바뀌므로 데이터가 쌓이기 전에 해야 싸다 |
| **2단계 — 보고 업무를 실제로 줄이는 것** | 1~2개월 | T10 여러 과제 묶음 보고 · T11 지난 보고 대비 변경분 · T7 vault 자동 이력 · T15 읽기 전용 공유 | 이 도구의 목적(보고)에 가장 가까운 것들. 여기서 정량 효과가 나온다 |
| **3단계 — 팀에서 조직으로** | 분기 | R2 여러 팀장 → R3 대표 아이템 → R1 성과 집계 | 순서를 바꾸면 앞 단계 데이터가 없어 뒤 단계가 헛돈다 |

> 발표용 한 줄: **"혼자 쓰는 도구 → 보고 업무를 줄이는 도구 → 팀이 함께 쓰는 도구"**

---

## 1. 지금 바로 (작고, 매일 체감)

| # | 항목 | 규모 | 효과 | 내용 |
|---|---|---|---|---|
| **T1** | **엑셀 표를 붙여넣으면 마크다운 표로 변환** | S | ★★★ | 지금은 엑셀 표를 붙이면 탭 구분 텍스트가 그대로 들어와 손으로 다시 그려야 한다. 붙여넣기가 표 모양(TSV)이면 md 표로 바꿔 넣는다. **실무에서 가장 자주 하는 동작** |
| **T2** | 진행일지 템플릿 | S | ★★ | 과제 개요는 템플릿이 있는데 진행일지는 빈칸에서 시작한다. 속성(R&D/투자/국책)별 기본 서식을 두면 무엇을 적을지 고민이 준다 |
| **T3** | **보고 초안 양식을 설정에서 편집** | S | ★★ | 지금 `## 보고 요약 / 특이사항 및 이슈 / 다음 계획` 이 **코드에 박혀 있다.** 회의체마다 양식이 다르면 바로 막힌다. 설정에서 고칠 수 있어야 하고, 회의체별로 다른 양식도 가능해야 한다 |
| **T4** | 지난 기록 이어쓰기 | S | ★★ | "지난번 내용을 가져와 새 기록 시작". 연속된 시험·측정에서 매번 같은 표를 다시 그리지 않아도 된다 |
| **T5** | **수행 이력에 보고 시점 마커** | S | ★★★ | **설계 문서 5.1에 적어 놓고 빠뜨린 것.** 타임라인에 "여기까지 2026-08-25 보고함" 선을 그으면, 목록을 보는 것만으로 미보고 분량이 눈에 들어온다. 이 도구의 핵심 가치와 가장 가까운 한 줄 |
| **T6** | **삭제 복구 화면** | S | ★★ | 지금 지우면 `.trash` 폴더를 탐색기로 직접 열어 되돌려야 한다. 실수 한 번이면 바로 필요해진다 |
| **T7** | 태그·그룹 자동완성 | S | ★ | 지금 자유 입력이라 `공정` / `공정개선` / `공정 개선` 이 따로 쌓인다. 시간이 지날수록 되돌리기 어렵다 |
| **T8** | **과제별 효과(정량·정성)와 산출 근거** | M | ★★★ | **개요 템플릿에 효과를 적을 자리가 아예 없다** — `배경·목표·산출물·관련 링크` 뿐이다. "그래서 회사에 얼마의 이익인가"가 빠져 있다. 상세에서 서술하고 목록에서도 한눈에 보이게 한다. 기대효과와 실적효과를 나눠 두어야 R1(성과 관리)이 선다. 스키마가 바뀌므로 **데이터가 쌓이기 전에 하는 편이 싸다.** → [TODO 26](TODO.md) |

---

## 2. 보고 업무를 실제로 줄이는 것

| # | 항목 | 규모 | 효과 | 내용 |
|---|---|---|---|---|
| **T10** | **여러 과제를 묶은 주간보고 한 장** | M | ★★★ | 지금은 과제마다 보고 문서를 하나씩 만든다. **실제 보고는 2~3건을 한 엑셀에 넣는다.** 고른 과제들을 한 문서로 합쳐 한 번에 복사할 수 있어야 한다. 이미 있는 "이번 주 보고 묶음"과 자연스럽게 이어진다 |
| **T11** | **지난 보고 대비 변경분 보기** | M | ★★ | "지난주에 보고한 것과 무엇이 달라졌나". 확정 스냅샷이 이미 남아 있으므로 비교만 하면 된다. 보고 자리에서 가장 많이 받는 질문에 대한 답 |
| **T12** | 보고 리마인더 | S | ★ | 월요일 선정 · 화요일 보고 주기에 맞춰 시작 화면에 배너. "오늘은 보고 대상을 고르는 날입니다 (미보고 3건)" |
| **T13** | 보고 이력 검색·필터 | S | ★ | 피보고자·회의체·기간으로 지난 보고를 찾는다. "그 회의체에 마지막으로 뭘 보고했더라" |
| **T14** | AI 보고 요약 제안 | M | ★★ | 미보고 진행일지를 요약해 초안의 `보고 요약` 칸을 **제안**한다. 다만 **확정은 반드시 사람이** — 보고 내용의 책임이 도구로 넘어가면 안 된다. 제안임을 화면에 분명히 표시하고, 손대지 않으면 저장되지 않게 한다 |
| **T15** | **읽기 전용 공유 내보내기** | M | ★★ | 서버 없이 팀원·상급자에게 보여 주는 방법. 과제 하나 또는 전체를 **정적 HTML 한 묶음**으로 내보내 메일·공유폴더로 전달한다. R2(서버 구축) 없이 "공유" 가치의 절반을 먼저 가져오는 저비용 수단 |

---

## 3. 데이터를 잃지 않기 위한 것

| # | 항목 | 규모 | 효과 | 내용 |
|---|---|---|---|---|
| **T20** | **vault 를 git 저장소로 두고 자동 커밋** | M | ★★★ | 데이터가 전부 텍스트 파일이라 git과 궁합이 완벽하다. 저장할 때마다 자동 커밋하면 **변경 이력·복구·"누가 언제 뭘 고쳤나"가 한 번에 해결**된다. 별도 이력 기능을 만드는 것보다 싸고 확실하다. R2(여럿이 사용)에서도 그대로 쓰인다 |
| **T21** | 자동 백업 스케줄 | S | ★★ | 지금은 설정에서 버튼을 눌러야 한다. 하루 한 번 지정 폴더로 자동 저장 |
| **T22** | watchdog 자동 감시 (보류 중) | M | ★ | 외부 편집기로 고친 파일을 [다시 읽기] 없이 반영. **실사용에서 [다시 읽기]를 자주 누르게 되는지 보고 판단**하기로 한 항목. T20을 하면 필요성이 줄어들 수도 있다 |
| **T23** | 대용량 확인 | S | ★ | 과제 300건 · 기록 5,000건 · 첨부 5GB 에서 화면이 버티는지 미리 확인. 지금 확인해 두면 나중에 급히 고칠 일이 없다 |

---

## 4. 여러 사람이 쓰기 위한 것 — R2 구체화

로드맵 R2를 실행 단위로 쪼갠 것. **위에서부터 순서대로** 해야 한다.

| # | 항목 | 규모 | 내용 |
|---|---|---|---|
| **T30** | 과제 번호 체계 정리 | S | 지금 `2026-001` 은 팀장이 여럿이면 겹친다. `2026-소재-001` 처럼 구분을 넣거나 서버가 발번한다. **데이터가 쌓이기 전에 정해야 싸다** |
| **T31** | 담당자 이름 ↔ 계정 매핑표 | S | 지금 담당자는 이름 문자열이다. 동명이인·표기 흔들림(`권경락` / `권 경락`)이 나중에 문제가 된다. 매핑표를 먼저 둔다 |
| **T32** | 사내 서버 배포 패키지 | M | 서버 한 대에 올리고 자동 시작(윈도우 서비스 등록), 포트·방화벽 안내까지 포함 |
| **T33** | 로그인 + 작성자 자동 기록 | M | 지금 작성자는 설정에서 고르는 값이다. 로그인이 생기면 자동으로 채워진다. **이 시점의 기록부터 R1 집계가 가능**해진다 |
| **T34** | 권한 | M | 내 과제는 수정, 남의 과제는 읽기. 팀장끼리는 서로 볼 수 있게 |
| **T35** | 동시 편집 대응 | M | 지금은 저장 직전 파일 수정 시각만 비교한다(1인 전제). 여럿이 같은 과제를 건드리면 부족하다 |

> **권장:** T30·T31 은 **지금 해 두는 게 싸다.** 데이터가 쌓인 뒤에 바꾸면 이관 작업이 붙는다.

---

## 5. 회의 운영과 성과 — R3 · R1 구체화

| # | 항목 | 규모 | 내용 |
|---|---|---|---|
| **T40** | **이번 주 보고 묶음을 서버에 저장** | S | 지금은 브라우저에만 저장돼 PC를 바꾸면 사라진다. R3의 전제 조건이자, 지금도 아쉬운 부분 |
| **T41** | 주차별 선정 기록 | M | 어느 주 회의에서 무엇이 뽑혔는지를 `selections/2026-W37/` 처럼 **사실로 남긴다.** 보고 확정 스냅샷과 같은 성격 |
| **T42** | 대표 아이템 목록 화면 | M | 선정된 과제만 모아 보고, 선정 이후 진행을 따로 추적 |
| **T43** | 기간별 활동 리포트 | M | 월·분기 단위로 완료 과제 수, 보고 횟수, 기한 준수율, 속성별 비중 |
| **T44** | 성과 정의 합의 | — | **코드보다 먼저 정해야 하는 것.** 무엇을 성과로 볼지 정하지 않으면 화면을 만들어도 아무도 안 본다. 완료 수 / 보고 횟수 / 기한 준수율 중 무엇인지 |

---

## 6. 눈에 안 보이지만 비용을 줄이는 것

| # | 항목 | 규모 | 내용 |
|---|---|---|---|
| **T50** | **화면 쪽 자동 테스트 도입** | M | 백엔드는 테스트 125건으로 지켜지는데 **화면은 0건이다.** 대시보드 칩 글자색이 안 보이던 결함(TODO 25)이 정확히 이 틈으로 샜다. 주요 화면이 열리는지, 색이 읽히는지 정도만 자동화해도 같은 종류의 사고를 막는다 |
| **T51** | 색상 명암비 자동 점검 | S | T50 안에 넣으면 거의 공짜. 선택/마우스올림 같은 상태 조합까지 자동으로 본다 |
| **T52** | 오류 기록 남기기 | S | 지금 화면에서 뭔가 잘못되면 흔적이 안 남는다. 파일로 남기면 원인 찾기가 빨라진다 |

---

## 7. 하지 않는 편이 낫다고 보는 것

고려했지만 **지금은 하지 말자**고 판단한 것들이다. 이유를 남겨 둔다.

| 항목 | 하지 않는 이유 |
|---|---|
| 구글독스식 실시간 공동 편집 | 파일이 원본이라는 이 도구의 근본 설계와 충돌한다. 얻는 것에 비해 잃는 것(단순함·이식성)이 크다 |
| 모바일 전용 앱 | 웹 반응형으로 충분하다. 진행일지 작성은 애초에 PC에서 하는 일이다 |
| 간트차트 · 과제 간 의존성 | 이 도구의 목적은 **이력과 보고**다. 일정 관리 도구가 되려 하면 둘 다 어중간해진다 |
| 결재·승인 워크플로 | 사내 시스템이 이미 하는 일이다. 겹치면 이중 입력만 늘어난다 |
| 자체 알림 메일 발송 | 사내 메일 정책에 묶인다. 화면 배너(T12)로 충분하다 |

---

## 8. 고를 때 참고

**효과 대비 비용이 가장 좋은 다섯 개** (제 판단)

1. **T8 과제 효과 항목** — 과제 관리 도구에 "그래서 얼마의 이익인가"가 없다. 빠진 축을 채운다
2. **T5 보고 시점 마커** — 반나절인데 이 도구의 핵심 가치를 눈에 보이게 만든다
3. **T1 엑셀 표 붙여넣기** — 가장 자주 하는 동작에서 손이 가장 많이 준다
4. **T10 여러 과제 묶음 보고** — 실제 보고 방식과 도구가 어긋나 있는 유일한 지점
5. **T20 vault git 자동 커밋** — 이력·복구·감사를 한 번에 해결. 별도로 만들면 훨씬 비싸다

(그다음이 **T50 화면 테스트** — 이미 한 번 사고가 났다. 두 번은 없어야 한다)

**빨리 정할수록 싼 것 두 개**

* **T30 과제 번호 체계**, **T31 담당자 이름 ↔ 계정 매핑**, **T8 과제 효과 항목**
  — 셋 다 데이터 구조가 바뀌는 일이다. 과제가 쌓인 뒤엔 이관 비용이 붙는다

**실사용 2~3주 뒤에 판단할 것**

* T22 watchdog — [다시 읽기]를 실제로 자주 누르게 되는지 보고
* T14 AI 요약 제안 — 보고 초안을 손보는 시간이 실제로 부담인지 보고
