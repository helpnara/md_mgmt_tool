# 외주 개발 시 비용 산정 — CRUD 기반 M/M 추정

> 2026-09-04 작성 · 목적: **바이브 코딩의 효과성 검증**
>
> 이 문서는 지금까지 만든 과제 관리 도구를 **외부 인원(SI 외주)에게 맡겼다면 얼마가 들었을지**를
> CRUD 행렬 → 기능점수 → M/M → 비용 순서로 산출하고, 실제 소요와 비교한 것입니다.
>
> **읽는 분께 미리 밝혀 둡니다.** 아래에서 **규모(줄 수, 끝점 수, 시험 건수)는 모두 실측치**이고,
> **단가(FP 단가, M/M 단가, 생산성)는 가정 파라미터**입니다. 단가는 「소프트웨어사업 대가산정 가이드」와
> 한국소프트웨어산업협회 「SW기술자 평균임금」의 **최신 공표치로 반드시 교체**한 뒤 인용하시기 바랍니다.
> 이 문서의 금액은 자릿수를 가늠하기 위한 것이지, 견적서가 아닙니다.

---

## 1. 요약

| 항목 | 값 |
|---|---|
| 기능점수(UFP, 간이법) | **319 FP** |
| 추정 공수 | **약 16 M/M** (범위 13~20) |
| 추정 외주 비용 | **약 1.6억 ~ 2.6억 원** (중간값 약 2억 원) |
| 실제 소요 기간 | 2026-09-01 ~ 09-04, 경과 **2일 16시간** / 실작업 **약 30시간** |
| 실제 소요 비용 | **약 250만 원** (본인 인건비 약 150만 + API 요금 약 100만) |
| 배수 | **약 1/80** |

한 줄로: **외주로 4~6개월 걸릴 규모를 사흘에, 자릿수로 두 자리 낮은 비용으로 만들었습니다.**
다만 이 배수는 "AI가 80배 빠르다"는 뜻이 아니라 **"요구자와 개발자가 같은 사람일 때 사라지는 비용"**이
얼마나 큰지를 함께 보여 주는 숫자입니다. 자세한 해석은 8장에 적었습니다.

---

## 2. 무엇을 얼마나 만들었나 (실측)

측정 시점 2026-09-04 05:53, 브랜치 `claude/team-task-management-tool-3d91g8`, 커밋 64개.

| 영역 | 파일 | 전체 줄 | 순수 코드(주석·빈 줄 제외) |
|---|---:|---:|---:|
| 백엔드 (Python / FastAPI) | 39 | 5,836 | 4,566 |
| 프런트엔드 (TypeScript / React) | 43 | 7,040 | 5,736 (TS) + 679 (CSS) |
| **제품 코드 소계** | **82** | **12,876** | **10,981** |
| 백엔드 시험 (pytest) | 29 | 4,189 | — |
| 화면 자동 시험 (Playwright) | 2 | 1,107 | — |
| 문서 (docs/) | 5 | 3,495 | — |
| **전체 합계** | **118** | **21,667** | — |

| 구조 지표 | 값 |
|---|---|
| API 끝점 | **56개** (GET 30 · POST 17 · DELETE 4 · PATCH 3 · PUT 2) |
| 저장 테이블 | 9개 + 전문검색 가상테이블 1개 (`search_fts`, FTS5 trigram) |
| React 컴포넌트 | 32개 |
| 주요 화면(라우트) | 6개 (과제 목록 · 과제 상세 · 보고 대상 · 보고 편집 · 검색 · 설정) |
| 자동 시험 | 백엔드 **320건** · 화면 **62건** (전부 통과) |

기능적으로는 과제·진행기록·보고·첨부의 관리, 주간/월간 보고서 초안 자동 생성, 지난 보고 대비 변경분,
보고 이력 검색, 전문 검색(한글 부분일치), 대시보드, 과제번호 일괄 재부여, 버전 보관(`.versions/`),
휴지통, 자동 백업, 오류 기록, 오프라인 설치 패키지(ZIP + 번들 wheel)까지 포함합니다.

---

## 3. CRUD 행렬

논리 데이터 단위(엔티티)별로 어떤 기능이 붙어 있는지를 실제 API 끝점에 대응시킨 표입니다.
괄호로 감싼 항목은 **다른 기능에 수반되어 자동으로 일어나는 것**(별도 끝점이 없음)입니다.

| # | 엔티티 | C (생성) | R (조회) | U (수정) | D (삭제) |
|---|---|---|---|---|---|
| 1 | 과제 (project) | `POST /api/projects` | `GET /api/projects`<br>`GET /api/projects/{id}`<br>`GET /api/dashboard`<br>`GET /api/projects/{id}/export` | `PATCH /api/projects/{id}` | `POST /api/projects/{id}/archive` |
| 2 | 진행기록 (entry) | `POST /api/projects/{id}/entries` | `GET /api/projects/{id}/entries`<br>`GET /api/entries/{id}` | `PATCH /api/entries/{id}` | `DELETE /api/entries/{id}` |
| 3 | 보고 (report) | `POST /api/projects/{id}/reports/draft` | `GET /api/reports`<br>`GET /api/reports/{id}`<br>`GET /api/projects/{id}/reports`<br>`GET /api/report-candidates`<br>`GET /api/reports/{id}/diff` | `PATCH /api/reports/{id}`<br>`POST /api/reports/{id}/freeze`<br>`POST /api/reports/{id}/unfreeze` | `DELETE /api/reports/{id}` |
| 4 | 첨부 (attachment) | `POST .../projects/{id}/attachments`<br>`POST .../entries/{id}/attachments`<br>`POST .../reports/{id}/attachments` | `GET` 목록 3종<br>`GET .../{id}/preview`<br>`GET .../{id}/thumb`<br>`GET /files/{dir}/{path}` | (파일 교체 = 재업로드) | `DELETE /api/attachments/{id}` |
| 5 | 담당자 명부 (people) | `POST /api/people` | `GET /api/people` | `PUT /api/people`<br>`POST /api/people/rename` | (명부에서 제외 = `PUT`) |
| 6 | 분류 (태그·상태·속성) | (과제 저장 시 동반) | `GET /api/meta` | (과제 저장 시 동반) | (동반) |
| 7 | 설정 (settings) | — | `GET /api/settings`<br>`GET /api/settings/defaults` | `PUT /api/settings`<br>`POST /api/settings/project-code/renumber` | — |
| 8 | 휴지통 (trash) | (보관 시 자동) | `GET /api/trash` | `POST /api/trash/{name}/restore` | (보관 기간 경과 시 자동) |
| 9 | 버전 (.versions) | (저장 시 자동) | `GET /api/versions`<br>`GET /api/versions/overview`<br>`GET /api/versions/content` | `POST /api/versions/restore` | (상한·보관일 초과 시 자동) |
| 10 | 백업 (backup) | `POST /api/settings/backup/run`<br>`GET /api/backup` (전체 내려받기) | `GET /api/settings/backup`<br>`GET /api/settings/backup/status` | `PUT /api/settings/backup` | (보관 개수 초과 시 자동) |
| 11 | 오류 기록 (errors) | (발생 시 자동) | `GET /api/errors` | — | `DELETE /api/errors` |
| — | 검색 색인 (search_fts) | `POST /api/reindex` | `GET /api/search` | (파일 저장 시 자동) | (재색인 시 재작성) |

**끝점 56개가 모두 매핑되었습니다.** 검색 색인은 마크다운 파일에서 파생되는 내부 구조물이므로
아래 기능점수 산정에서 **내부논리파일(ILF)로는 세지 않고**, 재색인·검색이라는 **거래 기능으로만** 반영했습니다.

---

## 4. 기능점수 산정 (간이법)

「소프트웨어사업 대가산정 가이드」의 **간이 기능점수법** 평균 복잡도 가중치를 적용했습니다.

| 유형 | 설명 | 개수 | 가중치 | 소계 |
|---|---|---:|---:|---:|
| ILF (내부논리파일) | 과제·진행기록·보고·첨부·담당자·분류·설정·휴지통·버전·백업·오류기록 | 11 | 7.5 | 82.5 |
| EIF (외부연계파일) | 외부 시스템 연동 없음 (사내 로컬 단독 실행) | 0 | 5.4 | 0.0 |
| EI (외부입력) | 데이터를 바꾸는 거래 — POST 16 · PUT 2 · PATCH 3 · DELETE 4 | 25 | 4.0 | 100.0 |
| EO (외부출력) | 계산·가공이 들어간 출력 — 대시보드, 검색, 보고 후보, 보고 차이, 내보내기, 백업 상태, 버전 개요, 썸네일/미리보기, 과제 목록(집계·정렬·연도필터), 전체 백업 ZIP, 번호 재부여 영향분석 | 12 | 5.2 | 62.4 |
| EQ (외부조회) | 단순 조회 — 상세 읽기, 목록 읽기, 설정 읽기, 파일 서빙 등 | 19 | 3.9 | 74.1 |
| | | | **UFP** | **319.0** |

> **보정 전 값입니다.** 대가산정 가이드는 여기에 규모·애플리케이션 유형·언어·품질 특성 보정계수를 곱합니다.
> 본 도구는 소규모 단독 실행형이라 보정계수가 1에 가깝다고 보고 **319 FP를 그대로** 사용했습니다.
> 실제 견적에서는 보정 후 값이 ±15% 정도 움직일 수 있습니다.

---

## 5. M/M 산출 — 세 방법으로 교차 검증

한 가지 방법만 쓰면 가정에 끌려가므로 세 가지로 각각 구하고 겹치는 구간을 봤습니다.

### (가) 기능점수법

국내 SI 프로젝트의 개발 생산성은 통상 **10~25 FP/M·M** 범위로 인용됩니다.
본 도구는 신규 개발(그린필드)이고 연동이 없어 상단, 대신 오프라인 설치·윈도우 환경 대응이라는
비정형 요구가 있어 중상단으로 잡아 **16~22 FP/M·M**을 적용합니다.

- 319 ÷ 22 = **14.5 M/M**
- 319 ÷ 16 = **19.9 M/M**
- 중앙 ≈ **17 M/M**

### (나) 코드 라인법

순수 제품 코드 **10,981줄**. SI 환경에서 설계·구현·시험·문서를 모두 포함한 산출 생산성은
현대 웹 스택 기준 **600~900 LOC/M·M** 정도로 봅니다.

- 10,981 ÷ 900 = **12.2 M/M**
- 10,981 ÷ 600 = **18.3 M/M**
- 중앙 ≈ **15 M/M**

### (다) 상향식 팀 구성법

실제로 이 정도 규모를 외주 발주했을 때 붙을 팀을 그려 본 것입니다.

| 역할 | 투입률 | 기간 | M/M |
|---|---:|---:|---:|
| PM / PL | 0.3명 | 4개월 | 1.2 |
| 분석·설계 (요구정의, 화면설계, 데이터 모델) | 1.0명 | 1.5개월 | 1.5 |
| 백엔드 개발 | 1.0명 | 3.0개월 | 3.0 |
| 프런트엔드 개발 | 1.0명 | 3.5개월 | 3.5 |
| 퍼블리싱 / UI | 0.5명 | 2.0개월 | 1.0 |
| QA / 시험 | 1.0명 | 2.0개월 | 2.0 |
| 배포·설치 패키징·매뉴얼 | 0.5명 | 2.0개월 | 1.0 |
| 소계 | | | **13.2** |
| 오프라인 설치·구버전 파이썬·사내망 제약 대응 가산 | | | +1.5 |
| **합계** | | | **약 14.7 M/M** |

### 종합

| 방법 | 범위 | 중앙 |
|---|---|---:|
| (가) 기능점수법 | 14.5 ~ 19.9 | 17 |
| (나) 코드 라인법 | 12.2 ~ 18.3 | 15 |
| (다) 상향식 | 13 ~ 17 | 14.7 |
| **채택** | **13 ~ 20 M/M** | **약 16 M/M** |

세 방법이 13~20 구간에서 겹칩니다. 이하 **16 M/M**을 기준값으로 씁니다.
개발 기간으로 환산하면 평균 3.5~4명 팀이 **4~5개월**입니다.

---

## 6. 비용 산출

**아래 단가는 전부 가정입니다.** 발표·보고에 인용하실 때는 최신 공표치로 바꿔 주십시오.

### (가) 시장 실거래 단가 기준

외주 개발 계약에서 흔히 쓰는 등급 혼합 월 단가를 **1인당 1,000만 원/M·M**(중급~고급 혼합, 제경비 포함)으로 가정.

| 공수 | 비용 |
|---|---:|
| 13 M/M | 1.3억 원 |
| **16 M/M (기준)** | **1.6억 원** |
| 20 M/M | 2.0억 원 |

### (나) 대가산정 가이드 — 투입공수 방식

`개발비 = 직접인건비 + 제경비 + 기술료` 구조를 그대로 적용.

| 항목 | 산식 | 월 환산 |
|---|---|---:|
| 직접인건비 | 가정 | 650만 원 |
| 제경비 | 직접인건비 × 110% | 715만 원 |
| 기술료 | (직접인건비 + 제경비) × 20% | 273만 원 |
| **M/M 단가** | | **1,638만 원** |

16 M/M × 1,638만 원 = **약 2.62억 원**

### (다) 기능점수 단가 방식

`개발원가 = 기능점수 × FP 단가`. FP 단가를 **55만 원**으로 가정(보정계수 1.0).

319 FP × 55만 원 = **약 1.75억 원**

### 종합

| 방식 | 금액 |
|---|---:|
| (가) 시장 실거래 단가 | 1.6억 원 |
| (다) 기능점수 단가 | 1.75억 원 |
| (나) 대가산정 가이드 | 2.62억 원 |
| **추정 구간** | **1.6억 ~ 2.6억 원** |
| **중간값** | **약 2.0억 원** |

여기에 통상 별도로 붙는 것: **유지보수비 연 개발비의 10~15%**(연 2,000만~3,000만 원),
하자보수 기간 외 변경요청 건별 정산, 설치·교육 비용.

---

## 7. 실제 소요 (실측)

### 기간과 투입

| 항목 | 값 |
|---|---|
| 최초 커밋 ~ 최종 커밋 | 2026-09-01 13:33 ~ 2026-09-04 05:30 (커밋 기준), 세션 전체 경과 **2일 16시간 29분** |
| 커밋 | 64개 |
| 커밋이 발생한 시간 구간 | 27개 (시각 단위) → **실작업 약 30시간** |
| 사용자(팀장) 지시 횟수 | **98회** |
| 투입 인원 | **1명** (요구자 = 개발자 = 검수자) |

30시간을 M/M로 환산하면(1 M/M = 160시간) **약 0.19 M/M**입니다.

### AI 사용량과 요금

| 항목 | 값 |
|---|---:|
| API 응답 수 | 2,060회 |
| 입력 토큰 합계 | 830,179,147 |
| ├ 캐시 읽기 | 789,138,082 (95.1%) |
| ├ 캐시 쓰기 | 41,036,977 |
| └ 신규 입력 | 4,088 |
| 출력 토큰 | 2,642,208 |

Claude Opus 5 공표 요금(입력 $5.00 / 1M, 출력 $25.00 / 1M, 캐시 쓰기 1.25배, 캐시 읽기 0.1배) 기준:

| 구분 | 계산 | 금액 |
|---|---|---:|
| 캐시 읽기 | 789.1M × $0.50/1M | $394.57 |
| 캐시 쓰기 | 41.0M × $6.25/1M | $256.48 |
| 신규 입력 | 4,088 × $5/1M | $0.02 |
| 출력 | 2.64M × $25/1M | $66.06 |
| **합계** | | **약 $717** |

환율 1,400원 가정 시 **약 100만 원**입니다.
캐시 적중률이 **95.1%**였기에 이 금액에 그쳤습니다. 캐시 없이 전량 신규 입력이었다면
입력만 830M × $5/1M = $4,151(약 580만 원)이었을 것입니다.

### 총 실비

| 항목 | 금액 |
|---|---:|
| 본인 인건비 (30시간 × 5만 원/시간 가정) | 약 150만 원 |
| Claude API 요금 | 약 100만 원 |
| **합계** | **약 250만 원** |

---

## 8. 비교와 해석

| 구분 | 외주 개발 (추정) | 바이브 코딩 (실측) | 배수 |
|---|---:|---:|---:|
| 공수 | 약 16 M/M | 약 0.19 M/M | **84배** |
| 기간 | 4~5개월 | 3일 (실작업 30시간) | **약 45배** |
| 비용 | 약 2.0억 원 | 약 250만 원 | **약 80배** |

### 이 차이는 어디서 나왔나

**1) AI의 코드 생산 속도 — 전체의 일부일 뿐입니다.**
코드를 타이핑하는 시간은 원래도 SI 공수의 30~40% 정도입니다. 나머지는 회의, 문서, 검수입니다.

**2) 요구자와 개발자와 검수자가 같은 사람 — 여기가 가장 큽니다.**
외주 16 M/M 중 분석·설계 1.5, PM 1.2, QA 2.0 = **약 4.7 M/M(29%)**가 "무엇을 만들지 정하고,
서로 확인하고, 맞는지 검수하는" 비용입니다. 이번에는 그 왕복이 아예 없었습니다.
요구사항 문서도, 화면 설계서도, 검수 확인서도 쓰지 않았습니다. 만들고 바로 써 보고 바로 고쳤습니다.
실제로 이 문서를 쓰는 시점까지 발견된 실사용 결함은 2건(500 오류, 목록 깜빡임)이며,
둘 다 보고받은 당일에 원인 규명·수정·시험까지 끝났습니다. 외주였다면 이건 하자보수 티켓입니다.

**3) 요구가 처음부터 명확했습니다.**
만들 사람이 곧 매일 쓸 사람이었기 때문에 "이게 아닌데"가 발생하지 않았습니다.
SI에서 재작업(rework)은 통상 공수의 20~30%를 먹습니다.

### 정직하게 밝혀 둘 점

**(1) 산출물의 두께가 다릅니다 — 이번 쪽이 더 두껍습니다.**
자동 시험 382건(백엔드 320 + 화면 62)과 문서 3,495줄은 이 규모의 SI 발주에서 통상 나오는 수준보다
많습니다. 즉 외주에 **같은 수준의 시험·문서를 요구했다면 16 M/M보다 더 들었을 것**입니다.
이 점에서 위 배수는 **과소평가**입니다.

**(2) 반대로 기능점수는 끝점 기준이라 보수적입니다.**
주소로 화면 상태를 유지하는 처리, 표 정렬, 연도 필터, 과제 안 찾기 같은 **화면 내부 상호작용은
API 끝점이 없어 FP에 거의 반영되지 않았습니다.** 실제 FP는 319보다 클 가능성이 높습니다.
이 점에서도 위 배수는 **과소평가** 쪽입니다.

**(3) 그러나 "AI만으로 된 것"은 아닙니다 — 여기가 가장 중요합니다.**
98회의 지시에는 **팀장 본인의 업무 도메인 지식과 판단**이 그대로 들어가 있습니다.
보고 주기를 어떻게 잡을지, 어떤 컬럼이 실제 보고 자리에서 필요한지, 과제번호 체계를 어떻게 바꿀지,
git 자동 커밋이 사내망 정책상 왜 안 되는지 — 이런 판단은 AI가 대신하지 못했고,
그 판단이 없었다면 이 도구는 만들어지지 않았거나 만들어도 쓰이지 않았을 것입니다.
**배수 80배는 "AI가 80배 빠르다"가 아니라, "업무를 아는 사람이 직접 만들 수 있게 되면
조직에서 사라지는 비용이 이만큼"이라는 뜻으로 읽는 것이 맞습니다.**

**(4) 규모가 커지면 배수는 줄어듭니다.**
1인이 머릿속에 담을 수 있는 범위를 넘어가면(다중 사용자, 권한, 외부 연동, 대용량)
설계·검증 비용이 다시 생깁니다. 이 결과를 "모든 SI를 대체할 수 있다"로 확대하면 안 됩니다.
**적정 적용 구간은 "쓸 사람이 곧 만들 사람이고, 요구가 명확하고, 규모가 1~2인 범위"**입니다.

**(5) 단가는 전부 가정입니다.**
FP 단가 55만 원, 직접인건비 650만 원, 시장 단가 1,000만 원, 환율 1,400원 — 모두 가정값입니다.
발표에 쓰실 때는 「소프트웨어사업 대가산정 가이드」와 「SW기술자 평균임금」 최신 공표치로
교체하시고, 사내 실제 외주 계약 단가가 있다면 그것이 가장 설득력 있습니다.

---

## 9. 발표에 쓸 한 장 요약

> **319 기능점수 · 56개 API · 코드 1.1만 줄 · 자동 시험 382건**
>
> 외주로 맡겼다면 **3~4명이 4~5개월, 약 16 M/M, 약 2억 원**
> 실제로는 **1명이 3일, 약 0.19 M/M, 약 250만 원** (AI 요금 100만 원 포함)
>
> 차이의 절반은 AI의 코드 생산 속도이고,
> **나머지 절반은 "요구자와 개발자가 같은 사람일 때 사라지는 회의·문서·검수 비용"입니다.**

---

## 부록. 산정 근거 재현 방법

이 문서의 실측치는 아래 명령으로 다시 확인할 수 있습니다.

```bash
# 제품 코드 줄 수
find backend/app -name '*.py' | xargs wc -l | tail -1
find frontend/src -type f \( -name '*.ts' -o -name '*.tsx' -o -name '*.css' \) | xargs wc -l | tail -1

# 순수 코드(주석·빈 줄 제외)
find backend/app -name '*.py' | xargs cat | grep -vE '^\s*$' | grep -vE '^\s*#' | wc -l

# API 끝점 수와 목록
grep -rhoE '@router\.(get|post|put|patch|delete)\("[^"]*"' backend/app/api | sort | uniq -c

# 자동 시험 건수
python -m pytest backend/tests -q --collect-only | tail -1
node tests/ui/screens.mjs   # 마지막 줄에 통과 건수가 찍힌다

# 커밋 수와 기간
git log --oneline | wc -l
git log --format='%ad' --date=format:'%Y-%m-%d %H:%M' | tail -1
```
