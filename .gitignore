# 데이터 (저장소 최상위의 vault 폴더만 — 앞의 / 가 없으면 backend/app/vault 같은
# 소스 폴더까지 무시해 버린다)
/vault/
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm

# Python
__pycache__/
*.py[cod]
/.venv/
/venv/
.pytest_cache/
.ruff_cache/

# Node
/frontend/node_modules/
/frontend/.vite/
/frontend/tsconfig.tsbuildinfo

# OS / 편집기
.DS_Store
.idea/
.vscode/

# 프론트엔드 빌드 결과(frontend/dist)는 커밋한다
# — 윈도우에서 Node.js 없이 실행하기 위함
